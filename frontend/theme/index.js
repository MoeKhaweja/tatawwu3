import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#4BA0A4", // Change this to your desired primary color
    secondary: "#F77C47", // Change this to your desired secondary color
    tertiary: "#1A455F", // Change this to your desired tertiary color
    // You can also define other color properties here
  },
};

export default theme;
